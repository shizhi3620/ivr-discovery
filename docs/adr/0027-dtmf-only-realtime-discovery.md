# 首版实时发现只支持 DTMF 导航

当前授权目标只提供 DTMF 导航，因此首版实时 ASR 只驱动 DTMF 发送，不使用 TTS 进行语音选项或自由对话。若实时转写显示菜单只有语音选项、没有可识别 DTMF 按键，则标记为 unsupported-voice-menu 并安全停止。这样可以缩小控制面，避免把语音合成误当成用户意图。

## Consequences

- 实时决策只允许发送当前探索目标对应的 DTMF 键。
- 语音-only 菜单进入未支持状态，不能推断为叶子节点。
- TTS 代码保留，但本目标首版不用于导航。
