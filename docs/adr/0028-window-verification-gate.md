# 发现窗口必须通过完整性和时窗门禁才算已验证

发现窗口只有在正确时窗内运行、所有运行完成、实时 ASR 无未解决故障、预算未耗尽、探索前沿为空、所有分支都有明确终结状态时才算已验证。人工窗口允许人工边界作为合法终点；自助窗口出现人工边界表示时窗或路由判断异常，不能标记为已验证。任一条件不满足时，最终报告只能是草稿。

## Consequences

- `completed` 只表示运行结束，不表示窗口已验证。
- 技术性未知、语义性未知、语音-only 菜单和预算耗尽都会阻止最终报告。
- 最终报告必须列出每个必需窗口的验证状态和未验证证据。
