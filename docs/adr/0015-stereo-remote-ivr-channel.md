# 实时 ASR 只识别远端 IVR 声道

实时音频转发模块使用 stereo 模式，由本地中继选择远端 IVR 对应的音频通道并只将该通道发送给腾讯实时 ASR。本地上行、TTS 和 DTMF 控制音不进入实时识别文本，避免控制音频污染菜单判断。正式拨打前必须用本地回放或已知音频校准左右声道映射。

## Consequences

- 中继需要显式配置并验证远端 IVR 声道，不能默认第一个声道就是目标。
- 实时 ASR 文本只代表目标 IVR 的语音，便于检测提示结束和人工边界。
- 文件 ASR 仍记录完整双声道通话，用于审计和声道复核。
