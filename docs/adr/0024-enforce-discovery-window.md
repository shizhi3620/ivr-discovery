# 外呼前强制校验发现窗口

目标 IVR 可能因北京时间 21:00 前后而进入不同流程。项目决定使用 `Asia/Shanghai` 校验发现窗口，记录每次运行的实际开始/结束时间和匹配窗口。当前时间不属于所选窗口时默认拒绝外呼；只有显式人工覆盖并填写原因才能继续，且必须写入审计。报告必须区分时窗内实测和人工覆盖验证。

## Consequences

- 运行记录必须同时保存计划窗口和实际发生时间。
- 错误时段的流程不能标记为已验证，只能标记为人工覆盖或未验证。
- 跨午夜窗口需要显式定义结束时间，不能默认“21:00 后”永远属于同一日。
