# 普通发现只能续跑，增加呼叫预算必须显式审计

重复点击 Discover 不能重置或扩大发现窗口的呼叫预算。只要仍有剩余预算，Discover 从未完成的探索前沿继续；预算耗尽后不再发起外呼。增加上限必须通过独立的预算增加动作，输入新的累计上限并二次确认，记录操作者、时间、旧上限和新上限。这样可以避免误触放大呼叫量，也保留授权测试的责任边界。

## Consequences

- 预算按授权目标和发现窗口累计，而不是按页面点击或 WebSocket 重连计数。
- 页面刷新、服务重启和重新发现都不能重置预算。
- 预算耗尽时只能生成草稿报告，并列出尚未探索的节点和时间路由。
