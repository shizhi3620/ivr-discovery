# 实时 ASR 故障立即安全挂断

实时 ASR 断线、事件序列缺口或腾讯识别错误会移除实时决策能力。项目决定此时立即停止 DTMF、将探索呼叫标记为 technical-unknown 并安全挂断；文件 ASR 只用于事后复核，不能假装成实时结果驱动当前通话。按照技术性未知规则，同一路径最多允许一次预算内确认重试。

## Consequences

- 实时事件序列缺口必须触发停止，不允许继续等待或猜测按键。
- 文件 ASR 结果可用于节点审计，但不能改变当前通话已经执行的控制动作。
- 第二次实时故障后路径标记未完成，等待后续人工判断或预算增加。
