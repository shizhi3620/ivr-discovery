# 预算计入所有到达运营商或目标的外呼

预算必须反映真实外部影响，而不是只统计成功接通的呼叫。项目决定：所有到达运营商或目标系统的外呼，包括忙音、无人接听、拒接、人工安全挂断和技术性重试，都计入窗口预算。只有 originate 被本地校验拒绝、网关未注册或 FreeSWITCH 尚未产生外部 INVITE 的本地失败不计入预算。所有不计入预算的失败仍写审计原因。

## Consequences

- “失败”不能成为绕过预算重拨的通道。
- originate 前失败与外部呼叫失败必须使用不同状态和审计字段。
- 报告需要同时展示已用预算和未计入预算的本地失败。
