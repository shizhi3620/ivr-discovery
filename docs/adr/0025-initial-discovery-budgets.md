# 初始发现预算为时段 4/8、目标总计 12

基于已知 4006668800 的线性前置链路和未知的 21:00 后自助菜单，项目初始预算设为：21:00 前最多 4 条探索呼叫，21:00 后最多 8 条，同一授权目标总计最多 12 条。预算按真实探索呼叫累计，可在未耗尽时续跑；达到上限后必须显式增加预算并记录审计。

## Consequences

- 线性导航链中的多个 DTMF 步骤只消耗一条探索呼叫。
- 预算不会因页面刷新、服务重启或多次点击重置。
- 如果 21:00 后菜单复杂度超过 8 条探索呼叫，目标仍受 12 条总预算约束，必须显式增加预算。
