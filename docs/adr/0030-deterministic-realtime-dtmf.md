# 实时 DTMF 只使用确定性规则

实时通话控制不调用 DeepSeek。目标键识别、final ASR 判断、静音窗口、强关键词人工边界和未知边界停止都由确定性规则完成；DeepSeek 只用于通话后的结构化解析、节点标签和双语优化报告。这样可以避免模型延迟、限流和输出波动直接影响真实通话，并让按键和挂断行为可审计。

## Consequences

- 实时规则需要覆盖中文和英文的明确按键表达。
- 无法由规则确认目标键时必须安全停止，不能调用模型做自由猜测。
- DeepSeek 故障只影响通话后解析和报告，不影响正在进行的呼叫控制。
