# 阶段 4：真实根节点呼叫验证

日期：`2026-09-22 21:59 CST`

目标：`4006668800`

FreeSWITCH 频道：`986a7525-7b5a-43ba-a492-44371a0857ad`

录音：`/tmp/ivr-discovery-recordings/986a7525-7b5a-43ba-a492-44371a0857ad.wav`

## 执行条件

- 自助服务时窗（21:00-09:00）
- 单次呼叫，最长 45 秒
- 根节点只监听，不发送 DTMF
- Android WiFi 预检通过：同一 BSSID、驱动省电关闭、零丢包
- FreeSWITCH 单实例运行，频道已登记并可解析 UUID

## 结果

通话接通 `44.58` 秒。录音为双声道 `8 kHz / Int16`：

- 远端 IVR 声道 RMS `2210.6`，最大振幅 `20860`
- 本地声道 RMS `1.0`，符合只监听、未注入音频的预期
- 通话全程 BSSID 保持 `48:5f:08:47:07:6c`，Mac 到手机零丢包
- Android 日志持续产生 `GsmAudioPort` 音频帧，结束时由 FreeSWITCH 计划任务正常挂断

腾讯文件 ASR 返回：

```text
感谢您致电Apple。
为了给您提供最好的服务。
照Apple隐私政策的规定，按本次通话相关的部分有限个人信息。
可能会在中国大陆境外存储和处理。
如果您同意，请按1。
如需结束本次通话。
请按2。
没有检测到有效的选项。
为了给您提供最好的服务。
按照。
```

这确认了根节点的线性前置链路和第一个分叉：

- `1`：同意隐私政策并继续
- `2`：结束通话

阶段 4 通过。下一阶段必须使用实时 ASR 决策链路，在同一通电话内按顺序重放强制前缀并发送 `1`。
