# 4006668800 当前节点录音复核

复核时间：`2026-09-22 22:55 CST`

方法：

- 对当前 Run 的 5 条 WAV 重新执行腾讯文件 ASR。
- 应用 `ASR_PHRASE_CORRECTIONS_JSON` 中经用户确认的短语纠正。
- 使用 `deepseek-chat` 和 `dtmf_path` 上下文重新解析最终节点。

## 首句判定

根节点录音前 `5.0` 秒为静音；随后语音在 `5.02` 秒突然以高能量开始，没有
自然起音。用户试听确认，录音从“欢迎致电Apple”的“迎”开始，首个“欢”在采集
开始前已经播放并丢失。

因此这里是 GSM 音频桥启动延迟导致的**录音首字截断**；腾讯 `8k_zh`、`16k_zh`
之后又把残缺片段进一步误识别为“点Apple”或“1点Apple”。当前统一标记为：

```text
[录音起点缺失首字]迎致电Apple。
```

## 节点结果

| 路径 | call_id | 最终节点 | 录音结论 |
| --- | --- | --- | --- |
| root | `8e5b619d-5f90-4472-a682-3e9792aa1774` | Apple 隐私确认 | `1` 同意，`2` 结束 |
| 1 | `894aa3ad-b2c4-40dd-aec2-78c628a07a8c` | 语言选择 | 普通话按 `1`；For tech support in English, press `2` |
| 2 | `54a3d7b8-fac6-4679-98ba-57ab0adf5132` | 支持信息终结点 | 提示 Apple 支持 APP 和微信公众号，无后续菜单 |
| 1w1 | `e1012e02-dd9b-4e5b-892f-800aa8a680f6` | 非工作时间提示 | 仅“作为评估和培训客服人员，改进客服中心技术质量……请稍等”整句豁免人工判断 |
| 1w2 | `5d5c1401-8372-43c4-b806-134c61bb30ca` | 未进入英文分支 | 真实 ASR 仍未命中目标键；使用该录音做本地 pjsua 回放时，生产链路已连续成功发送 `1` 和 `2` |

## 已确认的 ASR 纠正

| ASR 原文 | 纠正后 |
| --- | --- |
| `点Apple。` | `感谢致电Apple。` |
| `Export in English.` | `For tech support in English, press 2.` |
| `Protect support in English.` | `For tech support in English, press 2.` |

## 当前状态

- 自助窗口已使用 `25/25`（已耗尽），`verified_at=NULL`、`unresolved_faults=1`。
- `1w2` 的真实呼叫已完成导航并验证：选择 English 后进入自动录音/质量评估
  提示并结束，没有新的 DTMF 菜单。
- `1w1` 尚未取得“请稍等”之后的内容。两次重探索都因实时链路把非工作时间
  句末的“请稍等”误判为人工边界而被挂断；缺陷已修复并覆盖回归测试，但
  该窗口预算已耗尽，非工作时间窗口也已于 `09:00` 关闭。
- `1w1` 的完整非工作时间句子是自动提示；其它包含“请稍等”的节点仍按人工
  风险处理。
- 最终报告继续保持 Draft。

英文分支的代码和录音回放验证已完成，但真实运营商实时 ASR 仍未向决策引擎
提供可用于 `1w2` 的最终目标键文本。

## 2026-09-23 非工作时间根节点复测

在 `07:32 CST` 再次只监听、不发送 DTMF 呼叫 `4006668800`：

- channel：`cfb1d58a-4c1d-43ae-829d-e71886c0d484`
- 实际听见完整 Apple 隐私确认：按 `1` 同意、按 `2` 结束
- 通话持续约 45 秒，没有播放“简短非工作时间提示后自动挂断”
- 本次仍然命中“隐私确认版本”，与“非工作时间只有简短提示”的假设不符

因此不能确认所有非工作时间通话都进入简短提示版本；可能确实存在版本分流，
但当前拨号、号码和时间条件未触发短提示版本。窗口预算已耗尽，本窗口不再
继续拨打。

## 2026-09-23 首音节修复复验

升级网关为“拨号前预热 mixer/PCM”后，再次只监听呼叫：

- channel：`2fc700c8-5cf3-4a91-baf0-bb43df8bad17`
- 预热耗时：`4644 ms`，完成后才拨 GSM
- ASR 首句：`感谢您致电Apple`

首个音节不再丢失，修复已生效。

## 2026-09-23 1w2 真实最终验证

正确续跑 `c69b05d5` 的 `1w2` 节点：

- channel：`4b47c737-5f90-4504-b993-44fbad1c2ae1`
- 已实时发送根节点 `1`
- 语言菜单阶段将静默完成窗口延长到 15 秒
- 仍产生 `unknown_boundary`，没有发送语言菜单键 `2`

相同录音在本地 pjsua 生产链路中能够正确发送 `1` 和 `2`，因此剩余问题集中在
运营商实时 ASR 对英文句子的 final 输出，而不是 DTMF 注入或纠正代码本身。

## 2026-09-23 英文 ASR 变体复验

在 `08:22 CST` 再次正确续跑 `1w2`：

- channel：`8a6e86b4-e912-4159-8a93-f76ce8deda85`
- 根节点 DTMF `1` 已发送
- 实时 ASR 的英文部分以 `Export in English` 形式出现
- 上一版兜底要求 `English + support/technical`，因此未发送 `2`

兜底规则已调整为：路径目标键为 `2` 且文本包含明确 `English` 时允许发送一次
`2`。仍需最后一次真实通话才能获取选择英文之后的录音。

## 2026-09-23 英文兜底复验

在 `08:29 CST` 再次正确续跑 `1w2`：

- channel：`ae06eaec-0852-423d-806b-5a215fd96e55`
- 根节点 DTMF `1` 已发送
- 文件 ASR 仍捕获到 `Export in English`
- 实时决策链没有收到包含 `English` 的 partial/final，因此英文兜底未触发
- DTMF `2` 未发送，仍没有英文分支录音

当前自助窗口预算已耗尽。继续前需要新的显式预算授权，并优先解决实时英文 ASR
未进入决策链的问题。

## 受控定时兜底

已实现仅针对 `1w2` 的定时兜底：

- `1` 仍由实时 ASR 触发。
- 等待英文目标键 16 秒。
- 超时后只发送一次 `2`，继续录音并进入下一节点。
- 其它路径不使用该兜底。

本地 Provider 回归已通过，后端 `135 passed`。真实英文录音已在后续调用中获得。

## 2026-09-23 英文分支录音

使用受控定时兜底完成 `1w2`：

- channel：`e6ac0b70-9ccd-4cad-b3b5-8898fcdd5f67`
- 根节点 `1` 由实时 ASR 触发：`08:44:42`
- 16 秒未收到英文目标键事件后，兜底发送 `2`：`08:44:58`
- 录音时长：`59.06` 秒
- 选择英文后进入自动录音/质量评估提示并结束，没有新的 DTMF 菜单
- `1w2` 节点已标记完成并在实时链路中验证
- `1w2` 子分支通过实时验证；自助 DiscoveryWindow 因 `1w1` 未完成仍未通过验证

音频：`/tmp/ivr-discovery-recordings/e6ac0b70-9ccd-4cad-b3b5-8898fcdd5f67.wav`

## 2026-09-23 普通话 `1w1` “请稍等”后续探索

用户确认 `1w1` 的“请稍等”不是人工提示，需要继续观察其后是否还有提示。
连续两次重探索都在该节点被实时链路挂断：

- channel：`0b658aa2-48cf-4df3-8627-9197bc2227e7`（`08:51 CST`）
- channel：`05d01416-806f-4dc8-b24e-07b2e99109b1`（`08:57 CST`）

两次录音都在“请稍等”附近被切断（第 24 通录音 `48.24` 秒，尾部仍在播放），
因此无法判断之后是否还有新提示。取证发现两个实时判定缺陷：

1. **片段级判定**：豁免规则要求整句“作为评估和培训客服人员……请稍等”在
   一次回调里出现，但实时 ASR 把它拆成多个片段，末段单独判定为人工边界。
2. **上下文丢失**：实时 ASR 把这些句子作为彼此独立的 partial 投递，仅保留
   最后一段同样会丢掉上文。

修复方式（提交 `ae9eb12`、`7538f8f`）：

- 实时决策改为维护分段滚动上下文：partial 若是上一段的扩展/修订则替换，
  否则作为新片段追加，人工边界判定使用整段拼接文本。
- 豁免正则的两处 `.{0,30}` 改为非贪婪，避免贪婪匹配跨过第一个“请稍等”
  吞掉后面的真实“转接人工”文本。
- 新增回归测试覆盖：final 拆分、partial 逐句投递、以及非工作时间句后仍出现
  真实转接时必须判为人工边界。

后端回归：`140 passed`。

音频：`/tmp/ivr-discovery-recordings/05d01416-806f-4dc8-b24e-07b2e99109b1.wav`

### 回放验证（不消耗通话预算）

用本地实时中继把第 25 通录音按 `remote_channel=0` 原速回放，走真实腾讯实时
ASR 与真实决策引擎：

- 事件：`partial 58` + `final 5`，**没有 `human_boundary`**，全程走完 48 秒录音
- 观察到真实分片形态：整句累积 partial，随后每句一个 final；`请稍等。` 作为
  独立的 final 句投递

用同一事件流做新/旧判定的 A/B：

```
输入：("……以作为评估和培训客服人员及改进客服中心技术质量之用", partial)
      ("请稍等。", partial)   # 长句尚未 final，请稍等已到
旧逻辑 -> human_boundary（在“请稍等”处误判）
新逻辑 -> 不误判
```

旧逻辑在长句仍是 partial 时被随后的 `请稍等` 覆盖，正是线上第 24、25 通的
失败模式；新逻辑保留分段上下文后不再误判。
